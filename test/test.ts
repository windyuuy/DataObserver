test("test",()=>{
    // 1、所有对象增加绑定后，将可以在任何地方通用
    // 2、因此可以替换整个对象
    // 问题：1、当整个对象替换时，如何刷新表达式的绑定
    //        本质上就是一个刷新过程，当对象或数值发生改变，必然重新执行，同时以来也会发生改变。
    //        相当于 依赖 仅仅起到一个watch的作用，之后将使用新的deps
})